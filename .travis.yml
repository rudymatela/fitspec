sudo: false

language: haskell

ghc:
  - "8.0"
  - "7.10"
  - "7.8"

matrix:
  include:
    - ghc: '7.6'
      script: >
        make      GHCFLAGS="-fno-warn-unrecognised-pragmas" &&
        make test GHCFLAGS="-fno-warn-unrecognised-pragmas"
    - ghc: '7.4'
      script: >
        make      GHCFLAGS="-fno-warn-unrecognised-pragmas" &&
        make test GHCFLAGS="-fno-warn-unrecognised-pragmas"

install:
  - cabal install --only-dependencies
  - cabal install haskell-src haskell-src-exts # <-- commented-out in cabal

script:
  - make && make test
  - cabal configure --enable-tests --enable-benchmarks && cabal build && cabal test
